name: pipeline
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: testing the python script
        uses: abrahamNtd/poc-git-action-python@master
        with:
          myInput: this is the input jajajaja
          anotherInput: optional