on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: python running form Docker
    steps:
      - name: create zip file
        id: create zip
        uses: abrahamNtd/poc-git-action-python@v4
        with:
          python-action: 'create zip'
          python-user: 'admin'